# URL del Google Apps Script desplegado como web app.
# Copiá esto a .env.local para desarrollo local con sync a Drive.
# En producción (GitHub Pages): Settings → Secrets → VITE_SCRIPT_URL
VITE_SCRIPT_URL=https://script.google.com/macros/s/TU_URL_AQUI/exec
